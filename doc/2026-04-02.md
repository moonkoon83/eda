# 2026-04-02 작업 기록

## 질문 및 요청
- 현재 프로젝트 분석 및 진행 단계 파악 요청
- 파악된 문제점(테스트 빌드 오류) 해결 및 전체 빌드 정상화 순차 진행

## 답변 및 수행 내용
### 1. 프로젝트 현황 분석
- "이벤트 기반 병렬 코레오그래피 사가 패턴" 도입 완료 확인.
- `KafkaTemplate` 도입으로 인한 `OrderServiceTest` 및 `OrderIntegrationTest` 빌드 오류 발견.

### 2. 테스트 코드 복구 및 고도화 (완료)
- **`OrderServiceTest.java`**: `ApplicationEventPublisher`를 `KafkaTemplate`으로 교체하고 Mock 의존성 추가하여 컴파일 에러 해결.
- **`OrderIntegrationTest.java`**:
    - 비동기 테스트를 위해 `Awaitility` 도입.
    - 단일 모듈 테스트를 위해 외부 서비스(Stock, Point)의 응답 이벤트를 Kafka로 직접 발행하는 로직 추가.
    - `PointDeductedEvent` 생성자 파라미터 불일치 수정.

### 3. 인프라 및 빌드 환경 정비 (완료)
- **`build.gradle`**: `awaitility` 및 `spring-kafka-test` 의존성 추가.
- **Docker**: `docker-compose`를 통해 MySQL 및 Kafka 인프라 재기동 (Zookeeper 상태 초기화 포함).
- **전체 빌드**: `./gradlew clean build`를 통해 모든 모듈의 정상 동작 확인 (BUILD SUCCESSFUL).

## 향후 과제
- `eda-stock-service` 및 `eda-point-service`의 실제 비즈니스 로직 연동 테스트.
- 다중 환경(K8s 등) 배포 전략 검증.
