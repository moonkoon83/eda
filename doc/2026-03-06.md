# 2026-03-06 작업 기록

## [진행 중] Kafka 도입 (2단계)
- **인프라 설정 완료**: `docker/docker-compose.yml`에 Zookeeper, Kafka, Kafka UI 추가 완료.
  - Kafka UI: `http://localhost:8888` 예정.
- **통신 설계**: `order.created`, `stock.deducted` 등 토픽 기반의 비동기 Saga 패턴 설계 중.

## [완료] 멀티 모듈 전환 (1단계)
- **모듈 분리**: `eda-common`, `eda-order-service`, `eda-stock-service`, `eda-point-service` 4개 모듈로 물리적 분리 완료.
- **코드 재배치**:
  - `eda-common`: 공통 이벤트 및 예외.
  - `eda-order-service`: 주문 도메인 및 API (Port: 8080).
  - `eda-stock-service`: 재고 도메인 (Port: 8081).
  - `eda-point-service`: 포인트 도메인 (Port: 8082).
- **빌드 시스템 개편**: 루트 `build.gradle` 및 각 하위 모듈별 `build.gradle` 설정 완료.
- **테스트 코드 복구**: 패키지 경로 및 임포트 수정 완료 (통합 테스트는 Kafka 미도입으로 인해 현재 실패 상태 유지).

## MSA 전환 전략 수립
- **통신 방식**: 현재의 병렬 코레오그래피(Choreography) Saga를 Kafka 메시징 기반으로 전환하기로 결정.
- **순서**: 멀티 모듈 전환 → Kafka 도입 → 서비스별 배포 순으로 진행.

## 세션 시작
- 일시: 2026년 3월 6일 금요일
- 목적: 프로젝트 구조 파악 및 MSA 전환 작업 시작.
