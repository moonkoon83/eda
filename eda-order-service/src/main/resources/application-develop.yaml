spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://${DB_HOST:localhost}:3306/eda?useSSL=false&serverTimezone=UTC&characterEncoding=UTF-8&allowPublicKeyRetrieval=true
    username: root
    password: root1234
  jpa:
    hibernate:
      ddl-auto: update # 개발 환경에서는 스키마 자동 업데이트를 유지합니다.
